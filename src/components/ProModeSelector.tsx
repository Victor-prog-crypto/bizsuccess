/**
 * Bizsaas v0.1 is a local-first BYOK desktop release.
 *
 * Pro/cloud modes are intentionally hidden until Bizsaas Pro is ready.
 * Keep this component as a no-op so existing imports do not break.
 */
export function ProModeSelector() {
  return null;
}
